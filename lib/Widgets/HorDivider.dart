import 'package:flutter/material.dart';

class HorDivider extends StatelessWidget {
   const HorDivider({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: 10,
      color: Colors.black,
    );
  }
}
